<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.eseo.dis.projet_android">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="Menu"
            android:name=".MenuActivity" />
        <activity
            android:name=".PostersActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ProjectsActivity"
            android:label="Projects"
            />
        <activity
            android:label="Details project"
            android:name=".ProjectDetailsActivity" />
        <activity
            android:label="My Juries"
            android:name=".MyJuriesActivity" />
        <activity
            android:label="Jury's Projects "
            android:name=".JuryDetailsActivity" />
        <activity
            android:label="Marks"
            android:name=".StudentNotationActivity" />
        <activity
            android:label="JPO"
            android:name=".JPOActivity" />
        <activity
            android:label="JPO"
            android:name=".JPOProjectDetailsActivity"></activity>
    </application>

</manifest>